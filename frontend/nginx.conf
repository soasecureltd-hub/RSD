server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Serve static assets directly
    location /assets/ {
        try_files $uri =404;
    }

    # All other routes go to index.html (SPA client-side routing)
    location / {
        try_files $uri /index.html;
    }
}
